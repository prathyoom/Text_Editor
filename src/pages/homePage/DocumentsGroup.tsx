// Addition of a Page to contain all stored documents

export const DocumentsGroup = () => {
  // Fetch call to retrieve all document previews from backend

  // Any adaption needed

  // Displaying the data and click to open a document ( moves to editor page )
  return <div></div>;
};
